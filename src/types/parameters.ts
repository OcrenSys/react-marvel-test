export type TParameters = {
    title?: string,
    titleStartsWith?: string,
    characters?: number,
    stories?: number,
    series?: number,
    events?: number,
    limit?: number,
    offset?: number,
}