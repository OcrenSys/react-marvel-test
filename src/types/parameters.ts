export type TParameters = {
    title?: string,
    titleStartsWith?: string,
    nameStartsWith?: string,
    characters?: number,
    comics?: number,
    stories?: number,
    series?: number,
    events?: number,
    limit?: number,
    offset?: number,
}